<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <queries>
        <intent>
            <action android:name="com.windusk.ecosystem.START_COMPONENT" />
        </intent>
    </queries>

    <permission
        android:name="com.windusk.ecosystem.CENTAUR_ECOSYSTEM_ACCESS"
        android:protectionLevel="normal" />

    <permission
        android:name="com.windusk.ecosystem.CONNECT_AS_SIGNED_COMPONENT"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="com.windusk.ecosystem.CENTAUR_ECOSYSTEM_ACCESS"/>

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WinduskEcosystem">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WinduskEcosystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TestSharingChoiserActivity"
            android:exported="false" />
    </application>
</manifest>